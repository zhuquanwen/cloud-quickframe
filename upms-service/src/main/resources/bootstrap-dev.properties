rabbitmq.host=localhost
rabbitmq.port=5672
rabbitmq.username=cloud
rabbitmq.password=cloud

#eureka.instance.instance-id=${spring.application.name}:${spring.application.instace_id:${server.port}}
eureka.instance.prefer-ip-address=true
eureka.instance.lease-expiration-duration-in-seconds=40
eureka.instance.lease-renewal-interval-in-seconds=20
eureka.client.service-url.defaultZone=http://iscas:iscas@localhost:1001/eureka/
eureka.client.registry-fetch-interval-seconds=10